{
  "name": "gamble-battle",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "typecheck": "npm run -ws typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
